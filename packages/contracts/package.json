{
  "name": "@pokedex/contracts",
  "version": "0.2.0",
  "description": "Stable cross-boundary contract: route registry, action strings, native bridge keys, MF shared declarations. Bundled into both the host and every federated remote.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc --noEmit"
  },
  "publishConfig": {
    "registry": "http://localhost:4873/"
  },
  "devDependencies": {
    "@reduxjs/toolkit": "^2.12.0",
    "react-redux": "^9.3.0",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "@reduxjs/toolkit": ">=2.10.0",
    "react": "*",
    "react-native": "*",
    "react-redux": ">=9"
  }
}
